spring:
  datasource:
    url: jdbc:h2:mem:board;DB_CLOSE_DELAY=-1;MODE=MYSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop # 테스트 끝나면 삭제
    show-sql: false
    properties:
      hibernate:
        format_sql: true